package com.glucode.about_you.engineers.models
/*
import com.glucode.about_you.engineers.EngineersFragment
import com.glucode.about_you.mockdata.MockData
import org.junit.Assert.*
import org.junit.Before
import org.junit.Test
//import org.mockito.MockitoAnnotations

class EngineersFragmentTest {

    private lateinit var fragment: EngineersFragment

    @Before
    fun setUp() {
       // MockitoAnnotations.openMocks(this) //To be fixed
        fragment = EngineersFragment()
    }

    @Test
    fun testSortByYears() {
        val sortedByYears = MockData.engineers.sortedBy { it.quickStats.years }

        fragment.setUpEngineersList(MockData.engineers.sortedBy { it.quickStats.years })

        assertEquals("Engineers should be sorted by years", sortedByYears, MockData.engineers.sortedBy { it.quickStats.years })
    }

    @Test
    fun testSortByCoffees() {
        val sortedByCoffees = MockData.engineers.sortedBy { it.quickStats.coffees }

        fragment.setUpEngineersList(MockData.engineers.sortedBy { it.quickStats.coffees })

        assertEquals("Engineers should be sorted by coffees", sortedByCoffees, MockData.engineers.sortedBy { it.quickStats.coffees })
    }

    @Test
    fun testSortByBugs() {
        val sortedByBugs = MockData.engineers.sortedBy { it.quickStats.bugs }

        fragment.setUpEngineersList(MockData.engineers.sortedBy { it.quickStats.bugs })

        assertEquals("Engineers should be sorted by bugs", sortedByBugs, MockData.engineers.sortedBy { it.quickStats.bugs })
    }
}


 */